/**
 * コンパイル、テストが正常動作するか確認する為に作ったサンプル
 */
export namespace Sample {
  /**
   * @param {string} message
   * @returns {string}
   */
  export const showMessage = (message: string) => {
    return message;
  };
}
